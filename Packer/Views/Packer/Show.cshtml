@using PackerTracker.Models

<!DOCTYPE html>
<html>
  <head>
    <title>Packer Tracker</title>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
  </head>
  <body>
    <h1>@Model.TripName</h1>
    @* @if (Model.Count == 0)
    {
      <p>Add pieces of gear required for your trip!</p>
    } *@
    @* <ul>
      @foreach (KeyValuePair<string, bool> entry in Model.Gear)
      {
        <li>@Model.Gear[entry]</li>
      }
    </ul> *@
    <a href='/packer/@Model.Id/addgear'>Add gear to your trip</a>
    
  </body>